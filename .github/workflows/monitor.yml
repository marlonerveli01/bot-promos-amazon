name: Automacao Promos Amazon

on:
  schedule:
    # Executa a cada 30 minutos
    - cron: '*/30 * * * *'
  # Permite disparar o teste manualmente pelo painel do GitHub
  workflow_dispatch:

permissions:
  # Permissão necessária para salvar e atualizar o arquivo posted_deals.txt
  contents: write

jobs:
  check-deals:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install requests feedparser

      - name: Executar robo Amazon
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
          AMAZON_TAG: ${{ secrets.AMAZON_TAG }}
        run: python main.py

      - name: Salvar historico
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          touch posted_deals.txt
          git add posted_deals.txt
          git diff --quiet && git diff --staged --quiet || (git commit -m "Atualiza historico [skip ci]" && git push)
